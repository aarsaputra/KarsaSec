apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-app
spec:
  template:
    spec:
      hostNetwork: true
      containers:
      - name: web
        image: nginx:1.24
        securityContext:
          privileged: true
          runAsNonRoot: false
